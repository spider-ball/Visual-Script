local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Создание основного ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "CustomGameUI"
screenGui.ResetOnSpawn = false
screenGui.IgnoreGuiInset = true
screenGui.Parent = playerGui

-- 1. КНОПКА "ИКОНКА 3" (ВСЕГДА ВИДНА)
local toggleButton = Instance.new("TextButton")
toggleButton.Name = "ToggleButton"
toggleButton.Size = UDim2.new(0, 70, 0, 70)
toggleButton.Position = UDim2.new(1, -30, 0, 30)
toggleButton.AnchorPoint = Vector2.new(1, 0)
toggleButton.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
toggleButton.Text = "\u{0418}\u{043A}\u{043E}\u{043D}\u{043A}\u{0430}\n3" -- Иконка 3
toggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleButton.Font = Enum.Font.GothamBold
toggleButton.TextSize = 14
toggleButton.ZIndex = 10
toggleButton.Parent = screenGui

local trCorner = Instance.new("UICorner")
trCorner.CornerRadius = UDim.new(0.5, 0)
trCorner.Parent = toggleButton

-- 2. КОНТЕЙНЕР МЕНЮ
local menuContainer = Instance.new("Frame")
menuContainer.Name = "MenuContainer"
menuContainer.Size = UDim2.new(0.8, 0, 0.65, 0)
menuContainer.Position = UDim2.new(0.5, 0, 0.5, 0)
menuContainer.AnchorPoint = Vector2.new(0.5, 0.5)
menuContainer.BackgroundTransparency = 1 
menuContainer.BorderSizePixel = 0
menuContainer.ClipsDescendants = true 
menuContainer.Parent = screenGui

-- buttonsContainer (создан ниже) имеет отступы Position(0,5,0,6) и Size(1,-10,0,40).
-- Чтобы граница серой/синей панели совпадала с границей "Иконка1"/"Раздел1",
-- нужен тот же отступ: 5 - 10/12
local COLUMN_OFFSET = 5 - 10/12

-- ==========================================
-- 3. СЕРАЯ ПАНЕЛЬ (Поднята вверх на половину верхней панели)
-- ==========================================

local greyPanel = Instance.new("Frame")
greyPanel.Name = "GreyPanel"
-- Высота = 100% минус 26 пикселей (половина верхней панели)
greyPanel.Size = UDim2.new(1, 0, 1, -26) 
-- Позиция: поднята вверх на 26 пикселей (половина от 52)
greyPanel.Position = UDim2.new(0, 0, 0, 26)
greyPanel.BackgroundColor3 = Color3.fromRGB(100, 100, 100) 
greyPanel.BorderSizePixel = 0
greyPanel.ZIndex = 1
greyPanel.Parent = menuContainer

local greyCorner = Instance.new("UICorner")
greyCorner.CornerRadius = UDim.new(0, 12) 
greyCorner.Parent = greyPanel

-- ==========================================
-- 4. СИНЯЯ ПАНЕЛЬ (начинается ровно под "Раздел 1")
-- ==========================================

local bluePanel = Instance.new("Frame")
bluePanel.Name = "BluePanel"
bluePanel.Size = UDim2.new(11/12, -COLUMN_OFFSET, 1, 0)
bluePanel.Position = UDim2.new(1/12, COLUMN_OFFSET, 0, 0)
bluePanel.BackgroundColor3 = Color3.fromRGB(0, 100, 150) 
bluePanel.BorderSizePixel = 0
bluePanel.ZIndex = 2
bluePanel.Parent = greyPanel

local blueCorner = Instance.new("UICorner")
blueCorner.CornerRadius = UDim.new(0, 12) 
blueCorner.Parent = bluePanel

-- Толщина всех разделительных линий в интерфейсе (общая переменная)
local DIVIDER_WIDTH = 4

-- Перегородка между серой и синей панелями
local greyBlueDivider = Instance.new("Frame")
greyBlueDivider.Name = "GreyBlueDivider"
greyBlueDivider.Size = UDim2.new(0, DIVIDER_WIDTH, 1, 0)
greyBlueDivider.Position = UDim2.new(1/12, COLUMN_OFFSET - DIVIDER_WIDTH/2, 0, 0)
greyBlueDivider.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
greyBlueDivider.BorderSizePixel = 0
greyBlueDivider.ZIndex = 3
greyBlueDivider.Parent = greyPanel

-- ==========================================
-- 5. РОЗОВЫЙ БЛОК (вплотную к правому краю перегородки)
-- ==========================================

local pinkBox = Instance.new("Frame")
pinkBox.Name = "PinkBox"
pinkBox.Size = UDim2.new(0, 80, 0, 60) 
-- СДВИГ НА 2 ПИКСЕЛЯ ВПРАВО (немного меньше ширины перегородки), чтобы коснуться её правого края без наложения
pinkBox.Position = UDim2.new(0, 2, 1, 0) 
pinkBox.AnchorPoint = Vector2.new(0, 1) 
pinkBox.BackgroundColor3 = Color3.fromRGB(255, 105, 180) 
pinkBox.BorderSizePixel = 0
pinkBox.ZIndex = 4
pinkBox.Parent = bluePanel

-- Острые углы (нет UICorner)

-- ==========================================
-- 6. ВЕРХНЯЯ ПАНЕЛЬ (12 КНОПОК)
-- ==========================================

local borderBase = Instance.new("Frame")
borderBase.Name = "BorderBase"
borderBase.Size = UDim2.new(1, 0, 0, 52)
borderBase.BackgroundColor3 = Color3.fromRGB(30, 30, 30) 
borderBase.BorderSizePixel = 0
borderBase.ZIndex = 5 
borderBase.Parent = menuContainer

local borderCorner = Instance.new("UICorner")
borderCorner.CornerRadius = UDim.new(0, 20)
borderCorner.Parent = borderBase

-- FIX: UICorner скругляет ВСЕ 4 угла borderBase, из-за чего низ панели
-- тоже скруглялся и сквозь него проглядывал цвет greyPanel/bluePanel,
-- создавая кривой стык с телом меню. Патчи "квадратят" нижние углы.
local function squareBottomCorner(pos)
	local patch = Instance.new("Frame")
	patch.Size = UDim2.new(0, 20, 0, 20)
	patch.Position = pos
	patch.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
	patch.BorderSizePixel = 0
	patch.ZIndex = 3
	patch.Parent = borderBase
end

squareBottomCorner(UDim2.new(0, 0, 1, -20))
squareBottomCorner(UDim2.new(1, -20, 1, -20))

local greenTop = Instance.new("Frame")
greenTop.Name = "GreenTop"
greenTop.Size = UDim2.new(1, -4, 1, -4) 
greenTop.Position = UDim2.new(0, 2, 0, 2) 
greenTop.BackgroundColor3 = Color3.fromRGB(80, 160, 80) 
greenTop.BorderSizePixel = 0
greenTop.ZIndex = 6
greenTop.Parent = borderBase

local greenCorner = Instance.new("UICorner")
greenCorner.CornerRadius = UDim.new(0, 18) 
greenCorner.Parent = greenTop

local blueMask = Instance.new("Frame")
blueMask.Name = "BlueMask"
blueMask.Size = UDim2.new(1, 0, 0.5, 0)
blueMask.Position = UDim2.new(0, 0, 0.5, 0)
blueMask.BackgroundColor3 = Color3.fromRGB(60, 60, 180) 
blueMask.BorderSizePixel = 0
blueMask.ZIndex = 7
blueMask.Parent = greenTop

local bottomBorder = Instance.new("Frame")
bottomBorder.Name = "BottomBorder"
bottomBorder.Size = UDim2.new(1, 0, 0, 2) 
bottomBorder.Position = UDim2.new(0, 0, 1, -2) 
bottomBorder.BackgroundColor3 = Color3.fromRGB(30, 30, 30) 
bottomBorder.BorderSizePixel = 0
bottomBorder.ZIndex = 8
bottomBorder.Parent = borderBase

-- Контейнер для 12 кнопок
local buttonsContainer = Instance.new("Frame")
buttonsContainer.Name = "ButtonsContainer"
buttonsContainer.Size = UDim2.new(1, -10, 0, 40)
buttonsContainer.Position = UDim2.new(0, 5, 0, 6) 
buttonsContainer.BackgroundTransparency = 1
buttonsContainer.ZIndex = 9
buttonsContainer.Parent = menuContainer

local listLayout = Instance.new("UIListLayout")
listLayout.FillDirection = Enum.FillDirection.Horizontal
listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
listLayout.VerticalAlignment = Enum.VerticalAlignment.Center
listLayout.Padding = UDim.new(0, DIVIDER_WIDTH)
listLayout.Parent = buttonsContainer

-- Divider добавляется только если addDivider = true (не после последней кнопки),
-- толщина = DIVIDER_WIDTH, как и у greyBlueDivider, и полностью заполняет
-- промежуток между кнопками (Padding тоже = DIVIDER_WIDTH)
local function createRedPanel(text, addDivider)
	local panel = Instance.new("TextButton")
	panel.Size = UDim2.new(1/12, -4, 0, 34) 
	panel.BackgroundColor3 = Color3.fromRGB(200, 50, 50) 
	panel.BorderSizePixel = 0
	panel.Text = text
	panel.TextColor3 = Color3.fromRGB(255, 255, 255)
	panel.Font = Enum.Font.GothamBold
	panel.TextScaled = true
	panel.ZIndex = 10
	panel.ClipsDescendants = false 
	panel.Parent = buttonsContainer
	
	local panelCorner = Instance.new("UICorner")
	panelCorner.CornerRadius = UDim.new(0, 6) 
	panelCorner.Parent = panel

	if addDivider then
		local separator = Instance.new("Frame")
		separator.Name = "Divider"
		separator.Size = UDim2.new(0, DIVIDER_WIDTH, 1.5, 0) 
		separator.Position = UDim2.new(1, 0, -0.25, 0) 
		separator.BackgroundColor3 = Color3.fromRGB(0, 0, 0) 
		separator.BorderSizePixel = 0
		separator.ZIndex = 11 
		separator.Parent = panel
	end
end

-- Юникод-эскейпы вместо литеральной кириллицы — защита от "кракозябр"
-- при загрузке скрипта через некоторые экзекьюторы (проблема кодировки UTF-8).
local IKONKA = "\u{0418}\u{043A}\u{043E}\u{043D}\u{043A}\u{0430}"
local RAZDEL = "\u{0420}\u{0430}\u{0437}\u{0434}\u{0435}\u{043B}"

local panelNames = {
	IKONKA .. "\n1",
	RAZDEL .. "\n1", RAZDEL .. "\n2", RAZDEL .. "\n3", RAZDEL .. "\n4", RAZDEL .. "\n5",
	RAZDEL .. "\n6", RAZDEL .. "\n7", RAZDEL .. "\n8", RAZDEL .. "\n9", RAZDEL .. "\n10",
	IKONKA .. "\n2"
}

for i, name in ipairs(panelNames) do
	createRedPanel(name, i < #panelNames)
end

-- 7. ЛОГИКА ОТКРЫТИЯ / ЗАКРЫТИЯ МЕНЮ
local isMenuOpen = true

toggleButton.MouseButton1Click:Connect(function()
	isMenuOpen = not isMenuOpen
	menuContainer.Visible = isMenuOpen
end)